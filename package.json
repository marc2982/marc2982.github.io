{
	"name": "nhl-playoffs-pool",
	"version": "1.0.0",
	"type": "module",
	"description": "NHL Playoffs Pool - Automated build system for yearly summaries",
	"scripts": {
		"build": "node scripts/build.js",
		"build:year": "node scripts/build.js --year",
		"build:all": "node scripts/build.js --all"
	},
	"engines": {
		"node": ">=18.0.0"
	}
}
